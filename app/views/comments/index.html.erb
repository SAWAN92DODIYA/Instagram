<!DOCTYPE html>
<html>
<head>
    <title>Page Title</title>
    <%= stylesheet_link_tag "all_comments" %>
     <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <script src='https://kit.fontawesome.com/a076d05399.js' crossorigin='anonymous'></script>
</head>
<body>
    <div id="parent_id">
        <div id="child_id">
            <h1><b>Comments</b></h1>
            <div>
                <% @comments.each do |comment| %>
                    <div class="profile-container">
                        <div id="profile_pic">
                            <% if comment.user.profile_pic.attached? %>
                                <%= image_tag comment.user.profile_pic, class: "profile-pic" %>
                            <% end %>
                        </div>
                        <h4 id="profile_name"><%= comment.user.profile_name %></h4>
                        <% if comment.user == current_user %>
                            <%= button_to post_comment_path(comment.post_id, comment.id), method: :delete, data: { turbo_confirm: "Are you sure?", turbo: false }, class: "btn-icon-delete" do %>
                                <i class="material-icons">delete</i>
                            <% end %>
                        <% end %>
                    </div>
                    <p><%= comment.body %></p>
                 
                <% end %>
            </div>
        </div>
    </div>
</body>
</html>
